name: Test Terraform Infrastructure

on: [push, pull_request]

jobs:
  build:
    name: Test Terraform Infrastructure
    runs-on: ubuntu-latest
    steps:
      - name: Set up Go 1.14
        uses: actions/setup-go@v2
        with:
          go-version: 1.14

      - name: Checkout code
        uses: actions/checkout@v2

      # - uses: actions/cache@v2
      #   with:
      #    path: ~/go/pkg/mod
      #    key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
      #    restore-keys: |
      #      ${{ runner.os }}-go-
      
      - uses: actions/cache@v2
        with:
         path: ~/go/pkg/mod
         key: github.com/gruntwork-io/terratest/modules/aws
         restore-keys: |
           github.com/gruntwork-io/terratest/modules/aws

      #- name: restore modules
      #  run: go get -v github.com/gruntwork-io/terratest/modules/aws
        #run: go get -v github.com/gruntwork-io/terratest
      
      - name: execute tests
        run: cd tests && go test -v -count=1
      # # Runs the Super-Linter action
      # - name: Run Super-Linter
      #   uses: github/super-linter@v3
      #   env:
      #     DEFAULT_BRANCH: testing
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}